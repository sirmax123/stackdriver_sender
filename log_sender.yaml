apiVersion: v1
kind: Pod
metadata:
  name: log-sender
spec:
  containers:
  - image: mmazur_log_sender
    name: log-sender
#    command: ["/bin/sh"]
#    args: ["-c", "while true; do  D=`date`; echo ${D}_heartbeat_msg;  sleep 120; done"]
    volumeMounts:
    - mountPath: /d
      name: test-volume
  volumes:
  - name: test-volume
    hostPath:
      path: /var/lib/docker
